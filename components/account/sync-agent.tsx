'use client';
/**
 * STUB (foundation). Track B replaces this with the sync agent
 * (docs/platform.md 4.9): it syncs local progress to the account after
 * sign-in and reports its status to lib/account-store.ts. Until then it
 * does nothing, and local progress is untouched.
 */
export function SyncAgent() {
  return null;
}
